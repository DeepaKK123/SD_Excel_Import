# SD Excel Import

SD Excel Import provides the Basic programs and Python scripts needed to import Excel data into an SD account. Copy both components to the account before running an import.

1. Copy the Basic Program files from `BP` to the `BP` folder in the SD account.
2. Copy the entire `scripts` folder to the SD account path.